<!--components/books-not-found/books-not-found.wxml-->
<view class="books-not-found-container">
  <view class="not-found-header" bindtap="onToggle">
    <text class="not-found-title">以下{{books.length}}本书籍未找到资源</text>
    <text class="toggle-btn">{{expanded ? '收起' : '展开'}}</text>
  </view>
  <view wx:if="{{expanded}}" class="books-list">
    <view
      wx:for="{{displayBooks}}"
      wx:key="index"
      class="book-item"
    >
      <view class="book-info">
        <text class="book-title">《{{item.title}}》</text>
        <text wx:if="{{item.author}}" class="book-author">{{item.author}}</text>
      </view>
      <text
        class="recommend-link"
        bindtap="onRecommendTap"
        data-title="{{item.title}}"
        data-author="{{item.author}}"
      >
        订购
      </text>
    </view>
  </view>
</view>
